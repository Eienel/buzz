# Rust / Anchor
target/
**/*.rs.bk
test-ledger/
.anchor/
# Node
node_modules/
.yarn/
dist/
# Solana keys (never commit real keys)
*.json.key
id.json
# OS
.DS_Store
Thumbs.db
history.json

# local run artefact: the feed writes here when DATA_DIR is unset
server/thoughts.jsonl
